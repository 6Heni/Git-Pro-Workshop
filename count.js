function count(age, city, month, job) {
    console.log(`The month of your birth is: ${month}`);
    console.log(`Your age is: ${age}`);
    console.log(`You live in: ${city}`);
    console.log(`You work as: ${job}`);
}